# Secrets / env — the OpenAI key lives ONLY as a Vercel Environment Variable,
# never in this repo. Keep any local env files out of git.
.env
.env.*
.env*.local
*.key

# Vercel / Node
.vercel
node_modules/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log
